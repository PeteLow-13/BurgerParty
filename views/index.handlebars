<h1>Burger Party!!!</h1>

<h2>Burgers to Eat!</h2>

<ul>
    {{#each burgers}}
        {{#unless devoured}}
            <span> {{this.id}} - {{ this.burger_name}}</span>
            <button type='button' class='devoured-button' id='devouredButton' data-id ='{{this.id}}'data-devouredBurger='{{devour}}'></button>
        {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that have been Devoured!</h2>

<ul>
    {{#each burgers}}
        {{#if devoured}}
            <span> {{this.id}} - {{ this.burger_name}}</span>
            <button type='button' class='delete-button' id='deleteButton' data-id ='{{this.id}}'data-devouredBurger='{{devour}}'></button>
        {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger:</h2>

<form class='create-form'>
    <div class='form-group'>
        <label for='bu'>Buger:</label>
        <input type='text' id='bu' name='burger'>
    </div>

    <button id='create' type='submit'>Create Burger</button>

</form>